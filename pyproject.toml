[dependency-groups]
dev = [
    "black",
    "isort",
    "pylint",
    "pytest",
    "pyyaml>=6.0",
]

[tool.black]
line-length = 88

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
addopts = "-q"
